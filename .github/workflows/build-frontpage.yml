name: Build and Push Crafteur Frontpage

on:
  push:
    branches: [ "main" ]
    paths:
      - 'frontpage/**'
      - '.github/workflows/build-frontpage.yml'

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: zipleix/crafteur-frontpage

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./frontpage
          file: ./frontpage/Dockerfile
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
